<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yc.eshop.user.mapper.AddressMapper">
    <update id="updateAddressUser" parameterType="com.yc.eshop.common.entity.Address">
        update address set tag=#{tag}, receive_username=#{receiveUsername}, receive_telephone=#{receiveTelephone} where address_id=#{addressId}
    </update>

    <update id="updateAddress" parameterType="com.yc.eshop.common.entity.Address">
        update address set address=#{address} where address_id=#{addressId}
    </update>

</mapper>